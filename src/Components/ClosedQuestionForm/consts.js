const acceptedImageTypes = ['image/png', 'image/jpeg', 'image/gif', 'image/webp']
const [col1, col2, col3] = [1, 8, 3]

const answerPattern = "[A-Za-z0-9?]+"

export default {
  acceptedImageTypes,
  col1, col2, col3,
  answerPattern
}